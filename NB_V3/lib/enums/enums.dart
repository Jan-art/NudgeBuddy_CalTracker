
// This file contains all the enums used within the app when registering a new user.
// =================================================================================================

enum SignupSection{
  goals, // Weight loss, weight gain, weight maintain
  activityLevel, // Sedentary, lightly active, moderately active, very active, extra active (PAL VALUES)
  personalInfo, // Personal User Data
  submit 
}